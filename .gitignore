# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env
.env.local
.env.*.local
.env.vercel
.env.vercel.*
!.env.example
!.env.local.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# temporary files
tmp_*
tsc-output.txt
*.log

# build folders
prisma_build/
prisma_dist/
dist/
generated/

# backup files
backup_*.json
backup_*.js

# data dumps
db_dump.*
*.csv
*.xlsx

# processed PDFs
OMT*.pdf
*Despacho*.pdf
*.pdf

# shortcuts
*.lnk

# local database (SQLite)
prisma/local.db
prisma/local.db-journal
prisma/local.db*
prisma/dev.db
prisma/dev.db-journal

# logs de migração de dados (nao commitar)
prisma/logs/

# rclone crypt password (segredo local - nao commitar)
.rclone-crypt-pass.txt
prisma/instances/
prisma/backups/
/backups/
!src/app/backups/
launch.log
launch.err

# schema backup (needed for Render/GCP Cloud Run builds)
# prisma/schema.postgresql.prisma

SafeStation_1.0/
.vercel

# ferramentas de runtime local (nao commitar)
/bin

# dados de negocio / documentacao (nao commitar)
/CERTIFICADOS 2026/
/auditorias_documentos/
/manuais/
/documentacao/
*.docx
*.xls

# ficheiros de depuracao / temporarios
app_dev_err.txt
app_dev_log.txt
app_err.txt
app_log.txt
build_out*.txt
tsc-route-output.txt
usb_exclude.txt
gdrive
probe*.js
schema_probe.js
scratch-*.cjs
clean*.py
photo_mapping.json
populate_queue.js
run_sync_stock.cjs
add_missing_stock.cjs
fix_duplicate_references.cjs
_tmp_*.cjs
test-script.ps1

# Local-only seed scripts (contain secrets) - do not commit
src/seed-admin-user.cjs
